[
  {
    "original": "1. Call `get_goal` during intake or before the first execution loop when the prompt/hook says an active thread goal exists.\n2. If no goal exists, call `create_goal` only when the user or system explicitly asked for goal tracking; otherwise continue with Ralph state alone.\n3. Treat `goal.objective` as binding acceptance scope. Newer user updates can refine the current branch, but do not silently narrow the goal.\n4. Before completion, perform a prompt-to-artifact checklist and completion audit against real evidence:\n   - restate the objective as deliverables/success criteria\n   - map every prompt requirement, named workflow (`$ralplan`, `$ralph`), file, command, test, gate, and deliverable to evidence\n   - inspect the actual files, command output, state, and tests behind each checklist item\n   - identify missing, weakly verified, or uncovered requirements and continue if any remain\n5. Call `update_goal({status: \"complete\"})` only when the audit shows no required work remains. Do not use passing tests, Ralph state, or architect approval as proxy proof unless they cover the whole goal.\n6. If goal tools are unavailable, keep working through Ralph state and mention the missing goal-mode evidence in the final report.",
    "zh_tw": "1. 在 intake 期間，或在第一個 execution loop 之前，如果 prompt/hook 表示存在一個 active thread goal，請呼叫 `get_goal`。\n2. 如果不存在 goal，只有在使用者或系統明確要求 goal tracking 時才呼叫 `create_goal`；否則只使用 Ralph state 繼續。\n3. 將 `goal.objective` 視為具約束力的接受範圍。較新的使用者更新可以細化目前分支，但不要默默縮小 goal。\n4. 在完成前，根據真實證據執行 prompt-to-artifact 清單與 completion audit：\n   - 將 objective 重新表述為可交付成果/成功標準\n   - 將每個 prompt requirement、命名 workflow (`$ralplan`, `$ralph`)、檔案、指令、測試、gate 和可交付成果對應到證據\n   - 檢查每個 checklist 項目背後實際的檔案、命令輸出、狀態與測試\n   - 找出缺漏、驗證薄弱或未涵蓋的需求，若仍有任何項目未完成就繼續\n5. 只有在 audit 顯示沒有任何必需工作未完成時，才呼叫 `update_goal({status: \"complete\"})`。除非 passing tests、Ralph state 或 architect approval 覆蓋整個 goal，否則不要把它們當作替代證明。\n6. 如果 goal tools 無法使用，請繼續依 Ralph state 工作，並在最終報告中提及缺少 goal-mode 證據。"
  },
  {
    "original": "## State Management",
    "zh_tw": "## 狀態管理"
  },
  {
    "original": "Use the CLI-first state surface for Ralph lifecycle state (`omx state write/read/clear --input '<json>' --json`). Explicit MCP compatibility tools (`state_write`, `state_read`, `state_clear`) remain acceptable only when already enabled.",
    "zh_tw": "使用以 CLI 為先的狀態介面來管理 Ralph 的 lifecycle state（`omx state write/read/clear --input '<json>' --json`）。只有在已經啟用時，顯式的 MCP 相容工具（`state_write`、`state_read`、`state_clear`）才仍可接受。"
  },
  {
    "original": "- **On start**:\n  `omx state write --input '{\"mode\":\"ralph\",\"active\":true,\"iteration\":1,\"max_iterations\":10,\"current_phase\":\"executing\",\"started_at\":\"<now>\",\"state\":{\"context_snapshot_path\":\"<snapshot-path>\"}}' --json`\n- **On each iteration**:\n  `omx state write --input '{\"mode\":\"ralph\",\"iteration\":<current>,\"current_phase\":\"executing\"}' --json`\n- **On verification/fix transition**:\n  `omx state write --input '{\"mode\":\"ralph\",\"current_phase\":\"verifying\"}' --json` or `omx state write --input '{\"mode\":\"ralph\",\"current_phase\":\"fixing\"}' --json`\n- **On completion** (only after the completion audit passes with real evidence):\n  `omx state write --input '{\"mode\":\"ralph\",\"active\":false,\"current_phase\":\"complete\",\"completed_at\":\"<now>\",\"completion_audit\":{\"passed\":true,\"prompt_to_artifact_checklist\":[\"<requirement mapped to artifact/evidence>\"],\"verification_evidence\":[\"<fresh test/build/lint command and result>\"]}}' --json`\n- **Before the final answer**:\n  1. Run fresh verification and read the output.\n  2. Build `prompt_to_artifact_checklist` entries that map every user requirement, workflow gate, named file, command, PR/delivery requirement, and stop condition to a concrete artifact or evidence item.\n  3. Build `verification_evidence` entries with concrete commands, exit status, files inspected, PR URLs, or other machine-checkable evidence.\n  4. Write the Ralph completion state with a top-level `completion_audit` field on the Ralph state object. Do not write bare top-level `prompt_to_artifact_checklist` or `verification_evidence` fields by themselves; the Stop gate will reject them.\n  5. Read the state back with `omx state read --input '{\"mode\":\"ralph\"}' --json` and verify `completion_audit.passed === true`, a non-empty checklist, and non-empty verification evidence before producing the final answer.\n  6. If Codex goal mode is active, call `update_goal({status:\"complete\"})` only after this Ralph audit read-back succeeds.\n- **On cancellation/cleanup**:\n  run `$cancel` (which should call `omx state clear --input '{\"mode\":\"ralph\"}' --json`)",
    "zh_tw": "- **在開始時**：\n  `omx state write --input '{\"mode\":\"ralph\",\"active\":true,\"iteration\":1,\"max_iterations\":10,\"current_phase\":\"executing\",\"started_at\":\"<now>\",\"state\":{\"context_snapshot_path\":\"<snapshot-path>\"}}' --json`\n- **在每次迭代時**：\n  `omx state write --input '{\"mode\":\"ralph\",\"iteration\":<current>,\"current_phase\":\"executing\"}' --json`\n- **在驗證/修復轉換時**：\n  `omx state write --input '{\"mode\":\"ralph\",\"current_phase\":\"verifying\"}' --json` 或 `omx state write --input '{\"mode\":\"ralph\",\"current_phase\":\"fixing\"}' --json`\n- **在完成時**（僅在 completion audit 以真實證據通過之後）：\n  `omx state write --input '{\"mode\":\"ralph\",\"active\":false,\"current_phase\":\"complete\",\"completed_at\":\"<now>\",\"completion_audit\":{\"passed\":true,\"prompt_to_artifact_checklist\":[\"<requirement mapped to artifact/evidence>\"],\"verification_evidence\":[\"<fresh test/build/lint command and result>\"]}}' --json`\n- **在最終答案之前**：\n  1. 執行新的驗證並讀取輸出。\n  2. 建立 `prompt_to_artifact_checklist` 條目，將每個使用者需求、workflow gate、命名檔案、指令、PR/delivery 要求，以及停止條件對應到具體的 artifact 或證據項目。\n  3. 建立 `verification_evidence` 條目，包含具體指令、結束狀態、檢視的檔案、PR URL，或其他可由機器檢查的證據。\n  4. 在 Ralph state object 上寫入帶有頂層 `completion_audit` 欄位的 Ralph completion state。不要單獨寫入裸露的頂層 `prompt_to_artifact_checklist` 或 `verification_evidence` 欄位；Stop gate 會拒絕它們。\n  5. 使用 `omx state read --input '{\"mode\":\"ralph\"}' --json` 讀回 state，並在產生最終答案前驗證 `completion_audit.passed === true`、非空的 checklist，以及非空的 verification evidence。\n  6. 如果 Codex goal mode 啟用，只有在這次 Ralph audit 讀回成功後才呼叫 `update_goal({status:\"complete\"})`。\n- **在取消/清理時**：\n  執行 `$cancel`（它應該呼叫 `omx state clear --input '{\"mode\":\"ralph\"}' --json`）"
  },
  {
    "original": "## Scenario Examples",
    "zh_tw": "## 情境範例"
  },
  {
    "original": "**Good:** The user says `continue` after the workflow already has a clear next step. Continue the current branch of work instead of restarting or re-asking the same question.",
    "zh_tw": "**好：** 在 workflow 已經有明確下一步之後，使用者說 `continue`。請延續目前的工作分支，而不是重新開始或再次詢問同一個問題。"
  },
  {
    "original": "**Good:** The user changes only the output shape or downstream delivery step (for example `make a PR`). Preserve earlier non-conflicting workflow constraints and apply the update locally.",
    "zh_tw": "**好：** 使用者只變更輸出形式或下游交付步驟（例如 `make a PR`）。保留先前不衝突的 workflow 約束，並在本地套用更新。"
  },
  {
    "original": "**Bad:** The user says `continue`, and the workflow restarts discovery or stops before the missing verification/evidence is gathered.",
    "zh_tw": "**壞：** 使用者說 `continue`，而 workflow 重新開始探索，或在收集缺少的驗證/證據之前就停止。"
  },
  {
    "original": "<Examples>\n<Good>\nCorrect parallel delegation:\n```\ndelegate(role=\"executor\", tier=\"LOW\", task=\"Add type export for UserConfig\")\ndelegate(role=\"executor\", tier=\"STANDARD\", task=\"Implement the caching layer for API responses\")\ndelegate(role=\"executor\", tier=\"THOROUGH\", task=\"Refactor auth module to support OAuth2 flow\")\n```\nWhy good: Three independent tasks fired simultaneously at appropriate tiers.\n</Good>",
    "zh_tw": "<Examples>\n<Good>\n正確的平行委派：\n```\ndelegate(role=\"executor\", tier=\"LOW\", task=\"Add type export for UserConfig\")\ndelegate(role=\"executor\", tier=\"STANDARD\", task=\"Implement the caching layer for API responses\")\ndelegate(role=\"executor\", tier=\"THOROUGH\", task=\"Refactor auth module to support OAuth2 flow\")\n```\n原因：三個彼此獨立的任務同時以適當層級發出。\n</Good>"
  },
  {
    "original": "<Good>\nCorrect verification before completion:\n```\n1. Run: npm test           → Output: \"42 passed, 0 failed\"\n2. Run: npm run build      → Output: \"Build succeeded\"\n3. Run: lsp_diagnostics    → Output: 0 errors\n4. Delegate to architect at STANDARD tier  → Verdict: \"APPROVED\"\n5. Run /cancel\n```\nWhy good: Fresh evidence at each step, architect verification, then clean exit.\n</Good>",
    "zh_tw": "<Good>\n完成前的正確驗證：\n```\n1. Run: npm test           → Output: \"42 passed, 0 failed\"\n2. Run: npm run build      → Output: \"Build succeeded\"\n3. Run: lsp_diagnostics    → Output: 0 errors\n4. Delegate to architect at STANDARD tier  → Verdict: \"APPROVED\"\n5. Run /cancel\n```\n原因：每一步都有新的證據、架構師驗證，然後乾淨退出。\n</Good>"
  },
  {
    "original": "<Bad>\nClaiming completion without verification:\n\"All the changes look good, the implementation should work correctly. Task complete.\"\nWhy bad: Uses \"should\" and \"look good\" -- no fresh test/build output, no architect verification.\n</Bad>",
    "zh_tw": "<Bad>\n未經驗證就宣稱完成：\n\"All the changes look good, the implementation should work correctly. Task complete.\"\n原因：使用了 \"should\" 和 \"look good\" -- 沒有新的測試/建置輸出，也沒有架構師驗證。\n</Bad>"
  },
  {
    "original": "<Bad>\nSequential execution of independent tasks:\n```\ndelegate(executor, LOW, \"Add type export\") → wait →\ndelegate(executor, STANDARD, \"Implement caching\") → wait →\ndelegate(executor, THOROUGH, \"Refactor auth\")\n```\nWhy bad: These are independent tasks that should run in parallel, not sequentially.\n</Bad>\n</Examples>",
    "zh_tw": "<Bad>\n獨立任務的順序執行：\n```\ndelegate(executor, LOW, \"Add type export\") → wait →\ndelegate(executor, STANDARD, \"Implement caching\") → wait →\ndelegate(executor, THOROUGH, \"Refactor auth\")\n```\n原因：這些是應該平行執行而不是依序執行的獨立任務。\n</Bad>\n</Examples>"
  },
  {
    "original": "<Escalation_And_Stop_Conditions>\n- Stop and report when a fundamental blocker requires user input (missing credentials, unclear requirements, external service down)\n- Stop when the user says \"stop\", \"cancel\", or \"abort\" -- run `/cancel`\n- Continue working when the hook system sends \"The boulder never stops\" -- this means the iteration continues\n- If architect rejects verification, fix the issues and re-verify (do not stop)\n- If the same issue recurs across 3+ iterations, report it as a potential fundamental problem\n</Escalation_And_Stop_Conditions>",
    "zh_tw": "<Escalation_And_Stop_Conditions>\n- 當根本性阻礙需要使用者輸入時就停止並回報（缺少憑證、需求不清楚、外部服務中斷）\n- 當使用者說 \"stop\"、\"cancel\" 或 \"abort\" 時就停止 -- 執行 `/cancel`\n- 當 hook system 傳送 \"The boulder never stops\" 時繼續工作 -- 這表示迭代持續進行\n- 如果 architect 拒絕驗證，修正問題並重新驗證（不要停止）\n- 如果同一個問題在 3+ 次迭代中重複出現，將其回報為潛在的根本性問題\n</Escalation_And_Stop_Conditions>"
  }
]